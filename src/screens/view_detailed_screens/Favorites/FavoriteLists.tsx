import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FavoriteLists = () => {
  return (
    <View>
      <Text>FavoriteLists</Text>
    </View>
  );
};

export default FavoriteLists;

const styles = StyleSheet.create({});
